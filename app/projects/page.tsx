import ProjectClient from './ProjectClient';

const page = () => {
  return (
    <div className="pt-28">
      <ProjectClient projectName={''} projectDescription={''} projectId={''} />
    </div>
  );
};

export default page;
